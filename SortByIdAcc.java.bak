import java.util.Comparator;
class SortByIdAcc implements comparator
{
  public int compare(Object o1,Object o2)
  {
    Student s0 = (Student)o1;
	Student s2 = (Student)o2;
	if(s0.sid==s2.sid)
	return 0;
	else if(s0.sid>s2.sid)
	return 1;
	else
	  return -1;
  }
}